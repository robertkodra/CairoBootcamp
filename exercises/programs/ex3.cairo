from exercises.programs.ex1 import log_value

// Perform and log output of simple arithmetic operations
func simple_math() {
   // adding 13 +  14
   tempvar res = 13 + 14;
   log_value(res);

   // multiplying 3 * 6
   tempvar res = 3 * 6;
   log_value(res);

   // dividing 6 by 2
   tempvar res = 6 / 2;
   log_value(res);

   // dividing 70 by 2
   tempvar res = 70 / 2;
   log_value(res);

   // dividing 7 by 2
   tempvar res = 7 / 2;
   log_value(res);

    return ();
}